<!DOCTYPE html>
<html>
<head>
  <title>Profit and Loss Statement</title>
  <!-- Include any CSS or JavaScript files here -->
</head>
<body>
  <h1>Profit and Loss Statement</h1>
  
  <p>Start Date: <%= @start_date %></p>
  <p>End Date: <%= @end_date %></p>

  
  <!-- Add the form with the submit button -->
  <%= form_with(model: @profit_loss_statement, url: profit_loss_statements_path, method: :post) do |form| %>
    <div class="field">
      <%= form.label :revenue %>
      <%= form.number_field :revenue %>
    </div>

    <div class="field">
      <%= form.label :expenses %>
      <%= form.number_field :expenses %>
    </div>

    <div class="field">
      <%= form.label :sales %>
      <%= form.number_field :sales %>
    </div>

    <div class="field">
      <%= form.label :accounts_receivable %>
      <%= form.number_field :accounts_receivable %>
    </div>

    <div class="field">
      <%= form.label :accounts_payable %>
      <%= form.number_field :accounts_payable %>
    </div>

    <div class="actions">
      <%= form.submit "Submit" %>
    </div>
  <% end %>

<%= form_with(url: '#', method: :post) do |form| %>
  # ... form fields ...
  <div class="actions">
    <button type="submit">Show Report</button>
  </div>
<% end %>

<script>
  const form = document.querySelector('form');
  form.addEventListener('submit', async (event) => {
    event.preventDefault();

    const formData = new FormData(form);
    const response = await fetch('/profit_loss_statements', {
      method: 'POST',
      body: formData
    });

    if (response.ok) {
      const data = await response.json();
      // Update the table with the received data
      // (Use JavaScript to populate the table cells with values from data)
    } else {
      // Handle error scenario
    }
  });
</script>


  
</body>

</html>
